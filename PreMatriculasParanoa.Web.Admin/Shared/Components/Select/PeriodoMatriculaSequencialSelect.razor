@namespace PreMatriculasParanoa.Web.Admin.Shared.Components.Select

@using PreMatriculasParanoa.Domain.Models.Enumerations

<MudSelect T="EnumPeriodoMatriculaSequencial"
           Value="@Value"
           Label="@Label"
           ValueChanged="@OnValueChanged"
           Variant="@Variant"
           Margin="@(Dense ? Margin.Dense : Margin.Normal)"
           Dense="@Dense"
           Clearable="false">
    @{
        foreach (var item in Enum.GetValues<EnumPeriodoMatriculaSequencial>())
        {
            <MudSelectItem Value="@item">@item.EnumDescription()</MudSelectItem>
        }
    }
</MudSelect>

@code {
    [Parameter] public EnumPeriodoMatriculaSequencial Value { get; set; }
    [Parameter] public string Label { get; set; }
    [Parameter] public Variant Variant { get; set; } = Variant.Outlined;
    [Parameter] public bool Dense { get; set; } = false;
    [Parameter] public EventCallback<EnumPeriodoMatriculaSequencial> ValueChanged { get; set; }

    private void OnValueChanged(EnumPeriodoMatriculaSequencial value)
    {
        ValueChanged.InvokeAsync(value);
    }
}
