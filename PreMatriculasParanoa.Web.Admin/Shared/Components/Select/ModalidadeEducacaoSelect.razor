@namespace PreMatriculasParanoa.Web.Admin.Shared.Components.Select

@using PreMatriculasParanoa.Domain.Models.Enumerations

<MudSelect T="EnumModalidadeEducacao"
           Value="@Value"
           Label="@Label"
           ValueChanged="@OnValueChanged"
           Variant="@Variant"
           Margin="@(Dense ? Margin.Dense : Margin.Normal)"
           Dense="@Dense"
           Clearable="false">
    @{
        foreach (var item in Enum.GetValues<EnumModalidadeEducacao>())
        {
            <MudSelectItem Value="@item">@item.EnumDescription()</MudSelectItem>
        }
    }
</MudSelect>

@code {
    [Parameter] public EnumModalidadeEducacao Value { get; set; }
    [Parameter] public string Label { get; set; }
    [Parameter] public Variant Variant { get; set; } = Variant.Outlined;
    [Parameter] public bool Dense { get; set; } = false;
    [Parameter] public EventCallback<EnumModalidadeEducacao> ValueChanged { get; set; }

    private void OnValueChanged(EnumModalidadeEducacao value)
    {
        ValueChanged.InvokeAsync(value);
    }
}
